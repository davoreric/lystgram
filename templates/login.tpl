<section class="pageBlock">

	<header class="largeHead">
		<h1>lystgram</h1>
	</header>

	<section class="login">

		<a class="fb" ng-click="login()">{{ button }}</a>

		<p class="infoText">{{ message }}</p>

	</section>

</section>